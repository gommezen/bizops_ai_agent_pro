<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <title>{{ report.title }}</title>
  <style>
    body { font-family: Arial, Helvetica, sans-serif; margin: 24px; color:#222; }
    h1 { margin: 0 0 6px; } .muted{ color:#666; }
    h2 { border-bottom: 1px solid #ddd; padding-bottom: 6px; margin-top: 18px; }
    .card { border:1px solid #e5e5e5; border-radius: 8px; padding: 12px; margin: 10px 0; }
    table { border-collapse: collapse; width: 100%; }
    th, td { border: 1px solid #ddd; padding: 6px; text-align: left; }
    .center { text-align:center; }
    img { max-width: 100%; height: auto; }
  </style>
</head>
<body>
<h1>{{ report.title }}</h1>
<p class="muted">{{ report.client_name }} · {{ report.date }}</p>

<h2>Executive Summary</h2>
<p>{{ report.tldr }}</p>

<h2>Business Needs & Themes</h2>
<ul>
{% for t in needs.themes %}<li>{{ t }}</li>{% endfor %}
</ul>

<h2>KPIs</h2>
<table>
<thead><tr><th>KPI</th><th>Current</th><th>Target</th></tr></thead>
<tbody>
{% for k in needs.kpis %}<tr><td>{{ k.name }}</td><td>{{ k.current }}</td><td>{{ k.target }}</td></tr>{% endfor %}
</tbody>
</table>

<h2>Delivery Plan</h2>
<table>
<thead><tr><th>Phase</th><th>Weeks</th><th>Outputs</th></tr></thead>
<tbody>
{% for p in delivery.phases %}<tr><td>{{ p.name }}</td><td>{{ p.weeks }}</td><td>{{ ", ".join(p.outputs) }}</td></tr>{% endfor %}
</tbody>
</table>

<h2>Data Quality</h2>
<ul>
  <li>Rows: {{ dq.rows }}</li>
  <li>Columns: {{ dq.cols }}</li>
  <li>Missing % (median): {{ dq.missing_pct }}%</li>
  <li>Duplicates: {{ dq.duplicates }}</li>
  <li>Outlier estimate: {{ dq.outliers }}</li>
</ul>

<h2>Model</h2>
<p>{{ model.name }} · {{ model.validation }}</p>
<table>
<thead><tr><th>Metric</th><th>Value</th></tr></thead>
<tbody>
{% for m in model.metrics %}<tr><td>{{ m[0] }}</td><td>{{ m[1] }}</td></tr>{% endfor %}
</tbody>
</table>

{% if model.shap_path %}
<div class="card center">
  <img src="{{ model.shap_path }}" alt="SHAP summary"/>
  <p class="muted">SHAP global feature importance</p>
</div>
{% endif %}

<h2>Recommendations</h2>
<ol>{% for rec in recommendations.top %}<li><strong>{{ rec.title }}</strong> — {{ rec.text }}</li>{% endfor %}</ol>

<footer style="margin-top:24px; color:#777;">
  Generated by BizOps AI Agent Pro
</footer>
</body>
</html>
